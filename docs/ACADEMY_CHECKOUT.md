# Checkout Academy

Parcours: page cours → `/academy/checkout/[courseSlug]` → création `academy_payments` pending → `/academy/payment/mock/[paymentId]` en développement → success/cancel. Si `NEXT_PUBLIC_ACADEMY_CHECKOUT_ENABLED` n'est pas `true`, le fallback `/contact?type=academy-access&course=...` reste visible.
