import { redirect } from "next/navigation";
async function verify(formData: FormData){ "use server"; const id=String(formData.get("certificateId")??"").trim().toUpperCase(); if(id) redirect(`/certificats/verifier/${encodeURIComponent(id)}`); }
export default function VerifyCertificatePage(){ return <main className="min-h-screen bg-[#f8faf7] px-4 py-16"><div className="mx-auto max-w-xl rounded-3xl bg-white p-8 ring-1 ring-emerald-100"><p className="font-bold uppercase tracking-widest text-emerald-700">Certificats Agri-tech</p><h1 className="mt-3 text-3xl font-bold text-emerald-950">Vérifier un certificat</h1><form action={verify} className="mt-6 flex gap-3"><input name="certificateId" required placeholder="AGRITECH-CERT-2026-AB12CD" className="min-w-0 flex-1 rounded-xl border p-3"/><button className="rounded-xl bg-emerald-700 px-5 py-3 font-semibold text-white">Vérifier</button></form></div></main>}
