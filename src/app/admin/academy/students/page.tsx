/* eslint-disable @typescript-eslint/no-explicit-any */
import { requireAuthorizedAdmin } from "@/lib/auth/adminAuth"; import { createSupabaseAdminClient } from "@/lib/supabase/server";
export default async function StudentsAdmin(){ await requireAuthorizedAdmin(); const s=createSupabaseAdminClient(); const {data}=s?await s.from("profiles").select("*").eq("role","student").order("created_at",{ascending:false}):{data:[]}; return <div><h1 className="text-3xl font-bold">Étudiants</h1><div className="mt-6 overflow-x-auto rounded-2xl bg-white ring-1 ring-slate-200"><table className="w-full min-w-[640px] text-left text-sm"><tbody>{(data??[]).map((p:any)=><tr key={p.id} className="border-t"><td className="whitespace-nowrap p-3 font-semibold">{p.full_name??"Sans nom"}</td><td className="whitespace-nowrap p-3">{p.phone}</td><td className="whitespace-nowrap p-3">{p.organization}</td></tr>)}</tbody></table></div></div>}
