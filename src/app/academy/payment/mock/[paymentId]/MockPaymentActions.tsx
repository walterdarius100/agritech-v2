"use client"; import { useState } from "react";
export function MockPaymentActions({ paymentId }: { paymentId:string }) { const [loading,setLoading]=useState(""); async function send(result:string){ setLoading(result); const res=await fetch("/api/academy/payments/mock-confirm",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({paymentId,result})}); const data=await res.json(); window.location.href=data.redirectTo || (res.ok?"/academy/mes-cours":"/academy/payment/cancel"); } return <div className="mt-6 grid gap-3"><button onClick={()=>send("success")} disabled={!!loading} className="rounded-xl bg-emerald-700 px-5 py-3 font-bold text-white">Simuler paiement réussi</button><button onClick={()=>send("failed")} disabled={!!loading} className="rounded-xl bg-red-50 px-5 py-3 font-bold text-red-700">Simuler paiement échoué</button><button onClick={()=>send("cancelled")} disabled={!!loading} className="rounded-xl bg-slate-100 px-5 py-3 font-bold text-slate-700">Annuler</button></div> }
